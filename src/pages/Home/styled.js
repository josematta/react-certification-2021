import styled from 'styled-components';

const Section = styled.section`
  text-align: center;
`;

const Container = styled.div`
  display: flexbox;
  flex-direction: row;
  flex-wrap: wrap;
`;

const Styled = { Section, Container };
export default Styled;
